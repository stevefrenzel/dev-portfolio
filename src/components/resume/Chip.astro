---
export interface Props {
  name: string;
}

const { name } = Astro.props;
---

<span>{name}</span>

<style>
  span {
    background: var(--color-grey-material);
    border-radius: 5px;
    color: var(--color-black-material);
    font-family: "Atkinson Hyperlegible", serif;
    font-size: var(--step-1);
    padding: var(--space-3xs);
  }
</style>
